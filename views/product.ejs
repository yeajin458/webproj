<table class="table table-bordered border-primary table-hover" style="text-align: center;">
    <thead class="table-primary ">
        <th>상품</th>
        <th>상품명</th>
        <th>가격</th>
        <th>브랜드</th>
        <% if (cls == "MNG") { %>
        <th colspan="2"></th>
        <% } %>
    </thead> 

    <% {
    var i = 0;
    while (i < list.length) { %>
    <tr>
        
        <td><a href="/detail/<%=list[i].prod_id%>"><img src="/images/<%= list[i].image %>" alt="상품 이미지" width="100" /></a></td>
        <td><%=list[i].name%></td>
        <td><%=list[i].price%>원</td>
        <td><%=list[i].brand%></td>

        <% if (cls=="MNG"){%>
        <td><a href="/product/update/<%=list[i].prod_id%>" style="text-decoration: none;" >수정</a></td>
        <td><a href="/product/delete/<%=list[i].prod_id%>" style="text-decoration: none;"onclick="if(confirm('정말로 삭제하시겠습니까?')==false) return false">삭제</a></td>
        <%  } %>
    </tr>
      <% i++; } 
    } %>
</table>

<% if (list.length === 0) { %>
<p>자료가 없으니 코드 추가 버튼으로 코드를 추가해 주십시오.</p>

<% } %>

<% if (cls=="MNG"){%>
<a href="/product/create"><button class="btn btn-outline-secondary btn-sm" type="submit">상품 추가</button></a>
<% } %>